Градиентный спуск
Решить методом градиентного спуска уравнение  .
Решили на паре я отвечала.
Вопросы.
1.	Для чего используется градиентный спуск?
Ответ: алгоритм оптимизации, используемый для минимизации ошибок в модели машинного обучения
2.	Для какой функции L будем его применять в данной задаче?
Ответ: применяем модуль чтоб решить
3.	Чему равна производная L по x?
Ответ: ответ дан на паре
4.	Какие проблемы возникают при использовании градиентного спуска при решении этой задачи?
Ответ: --
### НАЧАЛО КОДА ###
# L = ... # Функция для которой применяем градиентный спуск

x = ... # Начальная точка
alpha = ... # Скорость градиентного спуска
max_iter = ... # Максимальное количество итераций
for _ in range(max_iter):
dL = ... # Производная этой функции
x -= alpha * dL
### КОНЕЦ КОДА ###
print(x, x ** 2 + 5 * x - 4)
 
 
train_set_x_flatten = train_set_x_orig.reshape(train_set_x_orig.shape[0], -1).T
test_set_x_flatten = test_set_x_orig.reshape(test_set_x_orig.shape[0], -1).T

s = 1/(1+ np.exp(-z))

w = np.zeros((dim, 1))
b = 0

1.	Part To Terrain
2.	Part To Terrain
3.	Building Tools от F3X


 

